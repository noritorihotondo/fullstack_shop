export { default as HTTPCode } from 'http-status-codes';

export enum HTTPMethod {
    GET = 'get',
    POST = 'post',
    PUT = 'put',
    PATCH = 'patch',
    OPTIONS = 'options',
    DELETE = 'delete'
}
